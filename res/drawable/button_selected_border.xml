<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" />
    <corners android:radius="8dp" /> <!-- Radio de las esquinas en dp -->
    <stroke
        android:width="6dp"
        android:color="@color/default_border_color" /> <!-- Color de borde por defecto -->
</shape>
