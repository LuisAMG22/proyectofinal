<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_selected="true">
        <shape>
            <solid android:color="@android:color/transparent" />
            <corners android:radius="8dp" /> <!-- Radio de las esquinas en dp -->
            <stroke
                android:width="6dp"
                android:color="#15D63E" /> <!-- Color del borde cuando está seleccionado -->
        </shape>
    </item>
    <item>
        <shape>
            <solid android:color="@android:color/transparent" />
            <corners android:radius="8dp" /> <!-- Radio de las esquinas en dp -->
            <stroke
                android:width="6dp"
                android:color="@color/default_border_color" /> <!-- Color del borde por defecto -->
        </shape>
    </item>
</selector>


